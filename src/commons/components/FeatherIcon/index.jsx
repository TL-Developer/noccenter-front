import React from 'react';

import {
  FeatherIconStyled,
} from './style';

const FeatherIcon = ({
  icon,
  size,
}) => (
  <FeatherIconStyled icon={icon} size={size} />
);

export default FeatherIcon;
