import styled from 'styled-components';
import Grid from '@material-ui/core/Grid';

export const FormStyled = styled(Grid)`
  background: ${({ theme }) => theme.color.neutral.light.pure};
`;

export const HeroStyled = styled(Grid)``;
